import { module, test } from 'qunit';
import { setupRenderingTest } from 'ember-qunit';

module('Integration | Component | subscription-feed', function (hooks) {
  setupRenderingTest(hooks);

  test.todo('it renders', function (assert) {
    assert.ok(false);
  });
});
